Durante todos los retos se trabajó con los datos para entender cómo se relacionan la temperatura y el oxígeno dentro del sistema.

Primero, se organizaron y revisaron los datos para tener claro qué información se estaba manejando. Esto ayudó a identificar las variables principales y ver cómo se comportaban en general.

Después, se utilizaron condicionales para clasificar los datos, por ejemplo, identificando cuándo el oxígeno estaba bajo o en niveles normales. Esto permitió tener una idea más clara del estado del sistema en cada momento.

También se usaron funciones para hacer el análisis más ordenado y evitar repetir procesos, lo que facilitó trabajar con los datos de manera más rápida y clara.

Luego, se aplicó un método sencillo que fue calcular la diferencia entre datos consecutivos usando .diff(). Con esto se pudo ver cómo iba cambiando el oxígeno en cada medición, identificando si estaba bajando o subiendo, y en qué momentos ocurrían esos cambios.

Finalmente, se realizó una gráfica para observar la relación entre la temperatura y el oxígeno. En esta gráfica se puede ver que cuando la temperatura aumenta, el oxígeno tiende a disminuir, lo cual coincide con lo que se esperaba teóricamente.

En general, todo el análisis permitió entender mejor los datos y comprobar que tienen un comportamiento lógico, ya que siguen una tendencia clara entre las variables
